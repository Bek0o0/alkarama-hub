{
  "name": "alkarama-hub",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix client run build",
    "postinstall": "npm --prefix client install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "json-server": "^0.17.3"
  }
}
